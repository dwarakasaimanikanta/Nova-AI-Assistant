# Nova AI Assistant - Python Dependencies
#
# This file only lists what is needed for the CURRENT phase
# (Phase 1: Project Foundation). Libraries for voice, memory,
# automation, and GUI will be added in later phases so the
# environment stays lightweight and easy to understand.
#
# Install with:
#   pip install -r requirements.txt

python-dotenv==1.0.1   # Loads environment variables from a .env file
rich==13.7.1            # Nicer console output (used from Phase 1 onward for CLI)
google-generativeai==0.8.3 # Google Gemini LLM API client SDK
ddgs==9.14.4 # DuckDuckGo search automation tool

# Voice input dependencies
sounddevice==0.5.5
numpy>=1.20.0
faster-whisper==1.2.1
